

export interface Addresses {
    id: string,
    address: string, // rua
    address_region: string, // bairro
    address_number: string, //number
    address_postalcode: string, // 8
    address_city: string,
    uf: string // (RJ, SP, MG)
    details:string
}